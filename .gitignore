# Python
__pycache__/
*.py[cod]
*.so

# Jupyter
.ipynb_checkpoints

# Editor / OS
.vscode/
.DS_Store
*.log

# Environment
.env
venv/

# Data (do NOT commit datasets to repository)
# (dataset/ was intentionally ignored before; including it now per user request)
level*/
patches_*/

# Cache, outputs
*.pthckpt
*.ckpt

# Ignore model caches but NOT .pth (we will track .pth with Git LFS)
# *.pth  <-- intentionally not ignored to allow Git LFS tracking
